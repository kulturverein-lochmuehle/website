---
import type { ResolvedPage } from '@/utils/page.utils.js';
import Section from '@/components/section.component.astro';

export type Props = ResolvedPage;

const { data } = Astro.props;
---

{data?.sections?.map(section => <Section {...section} />)}
